const express = require('express');
const app = express();
const port = 3000;
​app.use(express.json());
​app.post('/api/deposit', (req, res) => {
// Logic for creating deposit request
res.send('Deposit request created');
});
​app.post('/api/receipt/upload', (req, res) => {
// Logic for uploading receipt
res.send('Receipt uploaded');
});
